use crate::market_data::{MarketDataPublisher, MarketDataReader};
use crate::queue as mpsc;
use std::collections::{BTreeMap, HashMap};

use chrono::{NaiveDate, Utc};
use rust_decimal::Decimal;

use super::instrument::{Instrument, InstrumentMap};
use super::order::{Order, OrderId, OrderState, OrderType, Side};
use super::orderbook::{Book, OrderBook, RawTrade};
use super::stats::Statistics;

pub const QUOTE_ORDER_ID: OrderId = 0;

#[derive(Debug, PartialEq, Eq)]
pub enum EngineError {
    OrderNotFound,
    OrderIsNotActive,
    DuplicateOrderId(OrderId),
    InvalidOrderId(OrderId),
    InvalidQuantity,
    InvalidPrice,
    QuantityBelowCumulated,
    UnknownSymbol(String),
}

impl std::fmt::Display for EngineError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            EngineError::OrderNotFound => write!(f, "order not found"),
            EngineError::OrderIsNotActive => write!(f, "order is not active"),
            EngineError::DuplicateOrderId(id) => write!(f, "duplicate order id {}", id),
            EngineError::InvalidOrderId(id) => write!(f, "invalid order id {}", id),
            EngineError::InvalidQuantity => write!(f, "quantity must be positive"),
            EngineError::InvalidPrice => write!(f, "price must be positive"),
            EngineError::QuantityBelowCumulated => {
                write!(f, "replacement quantity is below already filled quantity")
            }
            EngineError::UnknownSymbol(s) => write!(f, "unknown symbol {}", s),
        }
    }
}

/// Execution event carried by an ExecutionReport.  This is deliberately
/// separate from `OrderState`: a replacement is an execution event whose
/// order remains active, so it must not turn the core order into a terminal
/// state merely to encode FIX ExecType=5/OrdStatus=5.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum ExecType {
    New,
    PartialFill,
    Fill,
    Cancelled,
    Expired,
    Replaced,
    Rejected,
}

/// a report pushed to a session's connection thread (order status or fill).
/// These are snapshots: no shared mutable state crosses threads.
#[derive(Clone, Debug)]
pub enum Report {
    Status {
        order: Order,
        symbol: String,
        /// Internal request ClOrdID to put on the wire.  It differs from
        /// `order.id` for an associated cancel request.
        cl_ord_id: OrderId,
        /// Fresh ExecID, unique across every report generated by an engine.
        exec_id: String,
        exec_type: ExecType,
        /// The order ID that the request superseded/cancelled, when present.
        orig_cl_ord_id: Option<OrderId>,
    },
    Fill {
        order: Order,
        symbol: String,
        last_price: Decimal,
        last_quantity: Decimal,
        /// Fresh ExecID, unique across every report generated by an engine.
        exec_id: String,
        /// Kept for a replacement fill when a client needs the lineage.
        orig_cl_ord_id: Option<OrderId>,
    },
}

/// An application-provided, bounded and nonblocking report destination.
/// Implementations must not perform network/disk IO or wait for capacity:
/// delivery runs in the Engine's serialized command path to preserve order.
pub(crate) trait ReportSink: Send + Sync {
    fn try_send(&self, report: Report) -> Result<(), std::sync::mpsc::TrySendError<Report>>;
}

impl ReportSink for mpsc::Sender<Report> {
    fn try_send(&self, report: Report) -> Result<(), std::sync::mpsc::TrySendError<Report>> {
        self.send(report)
    }
}

/// an incoming order request from a client
#[derive(Clone, Debug)]
pub struct NewOrder {
    pub id: OrderId,
    pub instrument_id: i64,
    pub side: Side,
    pub order_type: OrderType,
    pub price: Decimal,
    pub quantity: Decimal,
}

/// the client-side record of a quote pair (bid & ask); re-quoting removes the
/// old pair from the books. Mirrors quotePair.
#[derive(Default)]
struct QuotePair {
    bid: Option<Order>,
    ask: Option<Order>,
}

struct Session {
    orders: HashMap<OrderId, Order>,
    quotes: HashMap<i64, QuotePair>,
    sender: Box<dyn ReportSink>,
}

/// The exchange engine serializes trading state under its caller's lock.
/// Complete per-instrument snapshots are published independently for readers;
/// querying market data does not acquire the trading lock.
pub struct Engine {
    instruments: InstrumentMap,
    books: HashMap<i64, OrderBook>,
    sessions: BTreeMap<String, Session>,
    next_exchange_id: i32,
    next_trade_id: i64,
    next_exec_id: u64,
    next_arrival: u64,
    sequence: u64,
    market_data: MarketDataPublisher,
    pending_market_data: HashMap<i64, u64>,
    stats_cache: HashMap<i64, Statistics>,
    order_dates: HashMap<(String, OrderId), NaiveDate>,
    #[cfg(test)]
    snapshot_builds: usize,
}

impl Engine {
    pub fn new() -> Engine {
        Engine {
            instruments: InstrumentMap::new(),
            books: HashMap::new(),
            sessions: BTreeMap::new(),
            next_exchange_id: 0,
            next_trade_id: 0,
            next_exec_id: 0,
            next_arrival: 0,
            sequence: 0,
            market_data: MarketDataPublisher::new(),
            pending_market_data: HashMap::new(),
            stats_cache: HashMap::new(),
            order_dates: HashMap::new(),
            #[cfg(test)]
            snapshot_builds: 0,
        }
    }

    pub fn load_instruments(&mut self, path: &str) -> std::io::Result<()> {
        let result = self.instruments.load(path);
        // A read error may leave successfully parsed instruments in the map.
        // Publish the same registry the Engine exposes, including that case.
        let symbols = self.instruments.all_symbols();
        let instruments = &self.instruments;
        self.market_data.register(
            symbols
                .iter()
                .filter_map(|symbol| instruments.get_by_symbol(symbol)),
        );
        result
    }

    // --- sessions ---

    pub fn register_session(&mut self, id: &str, sender: mpsc::Sender<Report>) {
        self.register_session_sink(id, sender);
    }

    pub(crate) fn register_session_sink(&mut self, id: &str, sender: impl ReportSink + 'static) {
        let session = Session {
            orders: HashMap::new(),
            quotes: HashMap::new(),
            sender: Box::new(sender),
        };
        self.sessions.insert(id.to_string(), session);
    }

    pub fn session_ids(&self) -> Vec<String> {
        self.sessions.keys().cloned().collect()
    }

    /// cancels all orders and quotes of a session (mirrors SessionDisconnect)
    pub fn session_disconnect(&mut self, session_id: &str) {
        let Some(session) = self.sessions.remove(session_id) else {
            return;
        };
        self.order_dates.retain(|(owner, _), _| owner != session_id);
        let mut order_count = 0;
        let mut quote_count = 0;
        for (id, entry) in session.orders {
            if let Some(book) = self.books.get_mut(&entry.instrument_id) {
                if book.remove(session_id, id, entry.side, entry.price).is_ok() {
                    order_count += 1;
                }
            }
            self.record_market_data(entry.instrument_id, &[]);
        }
        for (instrument_id, pair) in session.quotes {
            for leg in [pair.bid, pair.ask] {
                if let Some(order) = leg {
                    if let Some(book) = self.books.get_mut(&instrument_id) {
                        if book
                            .remove(session_id, order.id, order.side, order.price)
                            .is_ok()
                        {
                            quote_count += 1;
                        }
                    }
                }
            }
            self.record_market_data(instrument_id, &[]);
        }
        self.publish_market_data();
        log::info!(
            "session {} disconnected, cancelled {} orders {} quotes",
            session_id,
            order_count,
            quote_count
        );
    }

    // --- instruments ---

    pub fn create_instrument(&mut self, symbol: &str) -> i64 {
        if let Some(instrument) = self.instruments.get_by_symbol(symbol) {
            return instrument.id;
        }
        let id = self.instruments.next_id();
        self.instruments.put(Instrument::new(id, symbol));
        self.market_data
            .register(std::iter::once(self.instruments.get_by_id(id).unwrap()));
        id
    }

    pub fn instrument_by_symbol(&self, symbol: &str) -> Option<&Instrument> {
        self.instruments.get_by_symbol(symbol)
    }

    pub fn all_symbols(&self) -> Vec<String> {
        self.instruments.all_symbols()
    }

    // --- orders ---

    pub fn order(&self, session: &str, id: OrderId) -> Option<&Order> {
        self.sessions.get(session)?.orders.get(&id)
    }

    pub fn expire_day_orders(&mut self) {
        self.expire_day_orders_at(Utc::now().date_naive());
    }

    fn expire_day_orders_at(&mut self, today: NaiveDate) {
        let expired: Vec<_> = self
            .order_dates
            .iter()
            .filter(|(_, date)| **date < today)
            .map(|(key, _)| key.clone())
            .collect();
        for (session, id) in expired {
            self.order_dates.remove(&(session.clone(), id));
            let Some(mut order) = self.order(&session, id).cloned() else {
                continue;
            };
            if !order.state.is_active() {
                continue;
            }
            let _ =
                self.remove_from_book(&session, id, order.side, order.price, order.instrument_id);
            order.state = OrderState::Expired;
            order.remaining = Decimal::ZERO;
            self.sync_record(&order);
            self.record_market_data(order.instrument_id, &[]);
            self.send_status_for(order, id, ExecType::Expired, None);
        }
        self.publish_market_data();
    }

    /// mirrors exchange.CreateOrder
    pub fn create_order(
        &mut self,
        session_id: &str,
        new: NewOrder,
    ) -> Result<OrderId, EngineError> {
        if self.instruments.get_by_id(new.instrument_id).is_none() {
            return Err(EngineError::UnknownSymbol(new.instrument_id.to_string()));
        }
        self.validate_order_input(new.id, new.order_type, new.price, new.quantity)?;
        {
            let session = self
                .sessions
                .get(session_id)
                .ok_or(EngineError::OrderNotFound)?;
            if session.orders.contains_key(&new.id) {
                return Err(EngineError::DuplicateOrderId(new.id));
            }
        }
        self.next_exchange_id += 1;
        self.next_arrival += 1;
        let mut order = match new.order_type {
            OrderType::Limit => Order::limit(
                session_id,
                new.id,
                new.instrument_id,
                new.side,
                new.price,
                new.quantity,
                self.next_arrival,
            ),
            OrderType::Market => Order::market(
                session_id,
                new.id,
                new.instrument_id,
                new.side,
                new.quantity,
                self.next_arrival,
            ),
        };
        order.exchange_id = self.next_exchange_id.to_string();

        let session = self
            .sessions
            .get_mut(session_id)
            .ok_or(EngineError::OrderNotFound)?;
        session.orders.insert(order.id, order.clone());
        self.order_dates
            .insert((session_id.to_string(), order.id), Utc::now().date_naive());

        let (mut trades, final_state) = self.add_to_book(order);
        self.record_market_data(new.instrument_id, &trades);
        self.publish_market_data();
        self.send_trade_reports(&mut trades);
        if let Some(final_order) = &final_state {
            self.sync_record(&final_order.clone());
            // mirror Go: status goes out when nothing traded, OR when a market
            // order's unfilled remainder was cancelled
            let remainder_cancelled =
                final_order.state == OrderState::Cancelled && new.order_type == OrderType::Market;
            if trades.is_empty() || remainder_cancelled {
                self.send_status(final_order.clone());
            }
        }
        Ok(new.id)
    }

    /// mirrors exchange.ModifyOrder: remove + re-add, so time priority resets.
    /// The replacement order takes the cancel-replace request's ClOrdID
    /// (new_id), and the Replaced report carries OrigClOrdID.
    pub fn modify_order(
        &mut self,
        session_id: &str,
        orig_id: OrderId,
        new_id: OrderId,
        price: Decimal,
        quantity: Decimal,
    ) -> Result<(), EngineError> {
        let order_type = {
            let session = self
                .sessions
                .get(session_id)
                .ok_or(EngineError::OrderNotFound)?;
            session
                .orders
                .get(&orig_id)
                .ok_or(EngineError::OrderNotFound)?
                .order_type
        };
        self.modify_order_with_type(session_id, orig_id, new_id, order_type, price, quantity)
    }

    /// Cancel/replace an active order, optionally changing its order type.
    /// The old internal ID is removed from the live order map and the new ID
    /// becomes the key.  Filled quantity and average price are carried over;
    /// the replacement quantity is the new total quantity, as required by FIX.
    pub fn modify_order_with_type(
        &mut self,
        session_id: &str,
        orig_id: OrderId,
        new_id: OrderId,
        order_type: OrderType,
        price: Decimal,
        quantity: Decimal,
    ) -> Result<(), EngineError> {
        let entry = {
            let session = self
                .sessions
                .get(session_id)
                .ok_or(EngineError::OrderNotFound)?;
            session
                .orders
                .get(&orig_id)
                .ok_or(EngineError::OrderNotFound)?
                .clone()
        };
        if !entry.state.is_active() {
            return Err(EngineError::OrderIsNotActive);
        }
        self.validate_order_input(new_id, order_type, price, quantity)?;
        if quantity < entry.cum_quantity {
            return Err(EngineError::QuantityBelowCumulated);
        }
        if new_id != orig_id
            && self
                .sessions
                .get(session_id)
                .is_some_and(|session| session.orders.contains_key(&new_id))
        {
            return Err(EngineError::DuplicateOrderId(new_id));
        }
        let instrument_id = entry.instrument_id;

        let _removed = match self.remove_from_book(
            session_id,
            orig_id,
            entry.side,
            entry.price,
            instrument_id,
        ) {
            Ok(removed) => removed,
            Err(_) => {
                // order exists in the session but not in the book: report and
                // ignore, mirroring the Go behavior
                let record = self
                    .sessions
                    .get(session_id)
                    .unwrap()
                    .orders
                    .get(&orig_id)
                    .unwrap()
                    .clone();
                self.send_status(record);
                return Ok(());
            }
        };
        // Preserve the logical removal version without constructing an
        // intermediate full book that will immediately be superseded.
        self.record_market_data(instrument_id, &[]);

        // A replacement is one FIX event.  Do not emit a preceding Cancelled
        // event for the old ClOrdID: the Replaced report carries OrigClOrdID.
        self.next_arrival += 1;
        let mut order = entry;
        order.id = new_id;
        order.order_type = order_type;
        order.price = price;
        order.quantity = quantity;
        order.remaining = quantity - order.cum_quantity;
        order.state = if order.remaining.is_zero() {
            OrderState::Filled
        } else if !order.cum_quantity.is_zero() {
            OrderState::PartialFill
        } else {
            OrderState::Booked
        };
        order.arrival = self.next_arrival;

        let session = self.sessions.get_mut(session_id).unwrap();
        session.orders.remove(&orig_id);
        session.orders.insert(new_id, order.clone());
        self.order_dates.remove(&(session_id.to_string(), orig_id));
        self.order_dates
            .insert((session_id.to_string(), new_id), Utc::now().date_naive());
        self.sessions
            .get_mut(session_id)
            .unwrap()
            .orders
            .insert(new_id, order.clone());

        self.send_status_for(order.clone(), new_id, ExecType::Replaced, Some(orig_id));

        // A replacement with no remaining quantity has no book entry and no
        // matching work.  It still receives the Replaced event above.
        if order.remaining.is_zero() {
            self.publish_market_data();
            return Ok(());
        }

        let (mut trades, final_state) = self.add_to_book(order);
        self.record_market_data(instrument_id, &trades);
        self.publish_market_data();
        self.send_trade_reports(&mut trades);
        if let Some(final_order) = &final_state {
            self.sync_record(&final_order.clone());
        }
        Ok(())
    }

    /// mirrors exchange.CancelOrder
    pub fn cancel_order(&mut self, session_id: &str, id: OrderId) -> Result<(), EngineError> {
        self.cancel_order_with_id(session_id, id, id)
    }

    /// Cancel an order using a fresh request ClOrdID.  The report references
    /// the cancelled order through OrigClOrdID while retaining the original
    /// order snapshot (including CumQty and AvgPx).
    pub fn cancel_order_with_id(
        &mut self,
        session_id: &str,
        orig_id: OrderId,
        cl_ord_id: OrderId,
    ) -> Result<(), EngineError> {
        self.validate_order_id(cl_ord_id)?;
        let entry = {
            let session = self
                .sessions
                .get(session_id)
                .ok_or(EngineError::OrderNotFound)?;
            session
                .orders
                .get(&orig_id)
                .ok_or(EngineError::OrderNotFound)?
                .clone()
        };
        if cl_ord_id != orig_id
            && self
                .sessions
                .get(session_id)
                .is_some_and(|session| session.orders.contains_key(&cl_ord_id))
        {
            return Err(EngineError::DuplicateOrderId(cl_ord_id));
        }
        if !entry.state.is_active() {
            return Err(EngineError::OrderIsNotActive);
        }
        let mut removed = self.remove_from_book(
            session_id,
            orig_id,
            entry.side,
            entry.price,
            entry.instrument_id,
        )?;
        let instrument_id = removed.instrument_id;
        if removed.state.is_active() {
            removed.state = OrderState::Cancelled;
        }
        self.sessions
            .get_mut(session_id)
            .unwrap()
            .orders
            .insert(orig_id, removed.clone());
        self.record_market_data(instrument_id, &[]);
        self.publish_market_data();
        self.send_status_for(removed, cl_ord_id, ExecType::Cancelled, Some(orig_id));
        Ok(())
    }

    /// mirrors exchange.Quote: a bid/ask pair per session and instrument, with
    /// replace semantics. A zero price skips (or withdraws) that side.
    #[allow(clippy::too_many_arguments)]
    pub fn quote(
        &mut self,
        session_id: &str,
        instrument_id: i64,
        bid_price: Decimal,
        bid_quantity: Decimal,
        ask_price: Decimal,
        ask_quantity: Decimal,
    ) -> Result<(), EngineError> {
        if self.instruments.get_by_id(instrument_id).is_none() {
            return Err(EngineError::UnknownSymbol(instrument_id.to_string()));
        }
        if bid_price != Decimal::ZERO && bid_quantity <= Decimal::ZERO {
            return Err(EngineError::InvalidQuantity);
        }
        if ask_price != Decimal::ZERO && ask_quantity <= Decimal::ZERO {
            return Err(EngineError::InvalidQuantity);
        }
        if bid_price < Decimal::ZERO || ask_price < Decimal::ZERO {
            return Err(EngineError::InvalidPrice);
        }
        let old_pair = {
            let session = self
                .sessions
                .get_mut(session_id)
                .ok_or(EngineError::OrderNotFound)?;
            session.quotes.remove(&instrument_id).unwrap_or_default()
        };
        for leg in [old_pair.bid, old_pair.ask] {
            if let Some(order) = leg {
                let _ = self.remove_from_book(
                    session_id,
                    order.id,
                    order.side,
                    order.price,
                    instrument_id,
                );
            }
        }

        let mut all_trades: Vec<RawTrade> = Vec::new();
        let mut pair = QuotePair::default();

        for (price, quantity, side, exchange_id) in [
            (
                bid_price,
                bid_quantity,
                Side::Buy,
                format!("quote.bid.{}", instrument_id),
            ),
            (
                ask_price,
                ask_quantity,
                Side::Sell,
                format!("quote.ask.{}", instrument_id),
            ),
        ] {
            if price.is_zero() {
                continue;
            }
            self.next_exchange_id += 1;
            self.next_arrival += 1;
            let mut order = Order::limit(
                session_id,
                QUOTE_ORDER_ID,
                instrument_id,
                side,
                price,
                quantity,
                self.next_arrival,
            );
            order.exchange_id = exchange_id;
            let (trades, _) = self.add_to_book(order.clone());
            all_trades.extend(trades);
            match side {
                Side::Buy => pair.bid = Some(order),
                Side::Sell => pair.ask = Some(order),
            }
        }
        self.sessions
            .get_mut(session_id)
            .unwrap()
            .quotes
            .insert(instrument_id, pair);

        self.record_market_data(instrument_id, &all_trades);
        self.publish_market_data();
        self.send_trade_reports(&mut all_trades);
        Ok(())
    }

    // --- book & statistics access (REST / console) ---

    /// Share access to published market data without sharing the Engine lock.
    pub fn market_data(&self) -> MarketDataReader {
        self.market_data.reader()
    }

    pub fn book(&self, symbol: &str) -> Option<Book> {
        let instrument = self.instruments.get_by_symbol(symbol)?;
        self.market_data
            .snapshot(instrument.id)
            .filter(|snapshot| snapshot.version != 0)
            .map(|snapshot| snapshot.book.clone())
    }

    pub fn statistics(&self, symbol: &str) -> Option<Statistics> {
        let instrument = self.instruments.get_by_symbol(symbol)?;
        self.stats_cache.get(&instrument.id).cloned()
    }

    // --- internals ---

    fn validate_order_id(&self, id: OrderId) -> Result<(), EngineError> {
        if id <= 0 || id == QUOTE_ORDER_ID {
            Err(EngineError::InvalidOrderId(id))
        } else {
            Ok(())
        }
    }

    fn validate_order_input(
        &self,
        id: OrderId,
        order_type: OrderType,
        price: Decimal,
        quantity: Decimal,
    ) -> Result<(), EngineError> {
        self.validate_order_id(id)?;
        if quantity <= Decimal::ZERO {
            return Err(EngineError::InvalidQuantity);
        }
        if price < Decimal::ZERO || (order_type == OrderType::Limit && price <= Decimal::ZERO) {
            return Err(EngineError::InvalidPrice);
        }
        Ok(())
    }

    fn next_exec_id(&mut self) -> String {
        self.next_exec_id = self.next_exec_id.saturating_add(1);
        format!("exec.{}", self.next_exec_id)
    }

    fn symbol_for(&self, instrument_id: i64) -> String {
        self.instruments
            .get_by_id(instrument_id)
            .map(|i| i.symbol.clone())
            .unwrap_or_default()
    }

    fn add_to_book(&mut self, order: Order) -> (Vec<RawTrade>, Option<Order>) {
        let instrument_id = order.instrument_id;
        let book = self
            .books
            .entry(instrument_id)
            .or_insert_with(|| OrderBook::new(instrument_id));
        book.add(order)
    }

    fn remove_from_book(
        &mut self,
        session_id: &str,
        id: OrderId,
        side: Side,
        price: Decimal,
        instrument_id: i64,
    ) -> Result<Order, EngineError> {
        let book = self
            .books
            .get_mut(&instrument_id)
            .ok_or(EngineError::OrderNotFound)?;
        book.remove(session_id, id, side, price)
            .map_err(|_| EngineError::OrderNotFound)
    }

    fn build_book(&mut self, instrument_id: i64) -> Book {
        #[cfg(test)]
        {
            self.snapshot_builds += 1;
        }
        self.books
            .entry(instrument_id)
            .or_insert_with(|| OrderBook::new(instrument_id))
            .build_book()
    }

    /// Keep every logical sequence advance and trade, but defer constructing
    /// full depth until the containing operation finishes. Intermediate
    /// versions of the same instrument can then be coalesced safely.
    fn record_market_data(&mut self, instrument_id: i64, trades: &[RawTrade]) {
        self.sequence += 1;
        self.pending_market_data
            .insert(instrument_id, self.sequence);

        let stats = self
            .stats_cache
            .entry(instrument_id)
            .or_insert_with(|| Statistics {
                symbol: self
                    .instruments
                    .get_by_id(instrument_id)
                    .map(|i| i.symbol.clone())
                    .unwrap_or_default(),
                ..Statistics::default()
            });

        for trade in trades {
            stats.volume += trade.quantity;
            if !stats.has_high_low {
                stats.high = trade.price;
                stats.low = trade.price;
                stats.has_high_low = true;
            } else {
                if trade.price > stats.high {
                    stats.high = trade.price;
                }
                if trade.price < stats.low {
                    stats.low = trade.price;
                }
            }
        }
    }

    /// Synchronously publish the last version of each changed instrument.
    /// Readers see a complete old or new snapshot; they do not see an
    /// intermediate removal during replace/disconnect/expiry. No background
    /// consumer or later market event is needed to flush the final state.
    fn publish_market_data(&mut self) {
        let mut pending = std::mem::take(&mut self.pending_market_data);
        for (instrument_id, sequence) in pending.drain() {
            let mut book = self.build_book(instrument_id);
            book.sequence = sequence;
            let stats = self.stats_cache.get_mut(&instrument_id).unwrap();
            let (bid_price, bid_qty) = book
                .bids
                .first()
                .map(|level| (level.price, level.quantity))
                .unwrap_or((Decimal::ZERO, Decimal::ZERO));
            let (ask_price, ask_qty) = book
                .asks
                .first()
                .map(|level| (level.price, level.quantity))
                .unwrap_or((Decimal::ZERO, Decimal::ZERO));
            stats.bid_price = bid_price;
            stats.bid_qty = bid_qty;
            stats.ask_price = ask_price;
            stats.ask_qty = ask_qty;
            self.market_data.publish(book, stats.clone());
        }
        // Reuse the small dirty map's capacity across commands.
        self.pending_market_data = pending;
    }

    /// assign the batch trade id and push fill reports to both sides of every
    /// trade (mirrors SendTrades + the shared tradeid per match batch)
    fn send_trade_reports(&mut self, trades: &mut [RawTrade]) {
        if trades.is_empty() {
            return;
        }
        self.next_trade_id += 1;
        let trade_id = self.next_trade_id;
        for trade in trades.iter_mut() {
            trade.trade_id = trade_id;
            self.send_fill(&trade.buyer, trade.price, trade.quantity);
            self.send_fill(&trade.seller, trade.price, trade.quantity);
        }
    }

    fn send_fill(&mut self, order: &Order, last_price: Decimal, last_quantity: Decimal) {
        // update the session's mirrored record for regular orders; quote legs
        // (id 0) are not tracked in session.orders
        if order.id != QUOTE_ORDER_ID {
            if let Some(record) = self
                .sessions
                .get_mut(&order.session_id)
                .and_then(|s| s.orders.get_mut(&order.id))
            {
                record.remaining = order.remaining;
                record.cum_quantity = order.cum_quantity;
                record.avg_price = order.avg_price;
                record.state = order.state;
            }
        }
        let symbol = self.symbol_for(order.instrument_id);
        let exec_id = self.next_exec_id();
        self.send(Report::Fill {
            order: order.clone(),
            symbol,
            last_price,
            last_quantity,
            exec_id,
            orig_cl_ord_id: None,
        });
    }

    /// keep the session's mirrored order record at the engine's final state
    /// (in Go this is a shared pointer, so it stays in sync automatically)
    fn sync_record(&mut self, order: &Order) {
        if order.id == QUOTE_ORDER_ID {
            return;
        }
        if let Some(record) = self
            .sessions
            .get_mut(&order.session_id)
            .and_then(|s| s.orders.get_mut(&order.id))
        {
            *record = order.clone();
        }
    }

    fn send_status(&mut self, order: Order) {
        let exec_type = match order.state {
            OrderState::Cancelled => ExecType::Cancelled,
            OrderState::Rejected => ExecType::Rejected,
            _ => ExecType::New,
        };
        self.send_status_for(order.clone(), order.id, exec_type, None);
    }

    fn send_status_for(
        &mut self,
        order: Order,
        cl_ord_id: OrderId,
        exec_type: ExecType,
        orig_cl_ord_id: Option<OrderId>,
    ) {
        let symbol = self.symbol_for(order.instrument_id);
        let exec_id = self.next_exec_id();
        self.send(Report::Status {
            order,
            symbol,
            cl_ord_id,
            exec_id,
            exec_type,
            orig_cl_ord_id,
        });
    }

    fn send(&mut self, report: Report) {
        let session_id = match &report {
            Report::Status { order, .. } => &order.session_id,
            Report::Fill { order, .. } => &order.session_id,
        };
        if let Some(session) = self.sessions.get(session_id) {
            // A full/disconnected destination marks its connection failed.
            // A committed fill is not rolled back; continue delivering to the
            // other parties rather than waiting for a slow destination.
            let _ = session.sender.try_send(report);
        }
    }
}

#[cfg(test)]
mod tests;
