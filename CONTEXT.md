# rust-trader

用 Rust 重写的模拟电子交易所（源自 go-trader）：撮合引擎 + 手写最小 FIX 4.2 会话层 + 只读 REST + client/playback 两个驱动端。

## Language

**契约基线**:
对外行为集合：FIX 消息流/端口、REST 端点与 JSON 字段、撮合规则与订单状态机。原定义为与 go-trader 完全一致（D2）；2026-09-13 起改为**可收缩**并完成首轮裁剪，不再承诺完全等价，最终清单见 docs/adr/0003。
_Avoid_: 契约等价、二进制可互换（描述当前承诺时）

**品种**:
一条可交易标的的记录，以数字 ID 与 Symbol 双重标识；初始来自 instruments.txt。
_Avoid_: 证券（英式语境）、单独的 symbol

**报价**:
做市会话对单一品种同时提交的买/卖双边挂单对；重新报价即替换上一对，价格 0 表示撤销该侧。
_Avoid_: MassQuote（那是线路消息名，不指概念本身）

**改单**:
一次改单 = 撤旧挂新：旧订单撤销、新订单入簿，时间优先级随之重置。
_Avoid_: replace、修改（未说明语义时）
