# Zed Diff 测试文件

用途：在 Zed 里测试 diff（gutter 标记 / 完整 diff 视图 / 折叠展开 hunk）。
测试方式：在 Zed 中打开本文件，随意改几行，然后看 gutter 与 git 面板。

## 1. 可编辑区（改这里最容易看到 hunk）

line-01 这一行留着别动
line-03 试着在下面插入一行
line-04 这一行已经改过了（修改型 hunk）
line-05 这一行改完后要能还原（Ctrl+Z）

v2-01 新增块：测试纯新增 hunk（1/3）
v2-02 新增块：测试纯新增 hunk（2/3）
v2-03 新增块：测试纯新增 hunk（3/3）

## 2. 连续块（测试相邻 hunk 是否合并）

      缩进行：测试纯空白变更是否被标记
        缩进八空格，测试层级变化
    tab 与空格的差异：	<- 这里是 tab
末尾多一个空格的差异：  
上一行行尾有两个空格

## 3. 无尾随换行 / 长行

这是一条很长的行，用来测试横向滚动与 diff 视图里长行的换行表现，重复填充填充填充填充填充填充填充填充填充填充填充填充填充填充填充。

## 4. 列表与代码块

- 项目 1
- 项目 2
  - 子项 2.1
- 项目 3（v2 在远端追加，测试独立 hunk）

```rust
fn main() {
    println!("hello zed diff");
}
```

## 5. 末尾标记

EOF（v2）
