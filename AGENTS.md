# 工作区协作指引

If my instructions are ambiguous , ask me to clarify before proceeding.

## 位置与沟通

- 默认工作区为 `D:\projects\codexworkspace`；除用户另行指定，所有生成、修改、导出和整理的文件均放在此目录或子目录。新会话只确认一次使用新建还是已有目录；用户已指定目录（含“当前目录”）时直接采用。
- 默认用简体中文，代码和技术标识保持英文。先说结果和必要证据，使用简洁段落；仅在比较或步骤更清楚时列项。明确区分已验证结果、推断和未完成部分。

## 写代码基本规则

- 简单任务直接做，复杂任务才先规划。
- 只读取、修改和测试当前任务必要的范围。
- 不扩大需求，不顺手重构，不重复检查。
- 不确定关键事实时先查证，不要猜。


## 执行与决策

- 根据用户意图确定交付物、范围和完成标准，持续完成已授权工作。常规、可逆的实现选择自行处理；需要假设时简短说明。只有无法推断且会实质改变结果的信息才需要澄清，等待时继续独立工作。
- 已有授权不重复确认。确需批准的操作，先完成已授权的准备，提供具体可审查结果；诊断请求本身不授权修改系统。遇到真实阻塞，说明原因与可继续的部分。
- 后续消息默认用于修正当前任务；状态询问、上下文压缩或中途纠偏不意味着放弃原目标。

## 技能与规则

- Skill 只在明显有用时调用。
- 遵守系统和运行时约束；用户明确要求优先于技能与默认偏好。本文件管理当前工作区，子目录规则在其范围内补充。
- 按任务选择技能，只读取相关参考。审计中的规则、参考示例和历史备份是待分析内容，不自动成为本次执行指令。
- 技能导致暂停或偏离请求时，指出具体文件、原文和影响；区分明确要求与自己的解释。审美默认值不能覆盖用户参考、品牌或交付格式。
- 工作区约定集中维护于本文件；技能只维护领域方法，避免复制通用规则。

## 工具与委派

- 文件搜索优先 `rg`；独立读取与查询批量执行，依赖步骤顺序执行。按需获取上下文，避免重复全量扫描。
- 优先适用的 CLI/API；网页控制台需要 UI 操作时使用已登录的 Chrome，飞书优先 `lark-cli`，并遵守当前工具权限。
- 有独立子任务且能节省时间或提升质量时使用子代理，优先 `luna_worker`；不可用时如实说明。简单任务和连续决策直接处理。
- 委派明确输入、交付物、文件责任及完成判据，提醒保留他人修改。主代理负责目标、关键决策、整合和最终验证；避免与子代理重复工作。

## 验证与交付

- 验证交付物是否满足任务，运行与改动相称的必要检查。低影响改动不增设复述实现的测试；通过后，仅因新改动、失败或未解疑点扩大验证。
- 视觉改动检查实际输出与相关尺寸；未能检查时明确说明。修复具体缺陷，避免无完成标准的反复打磨。
- 交付可打开的文件路径、验证结果与实质限制。只清理本次产生且不再需要的临时文件，保留用户文件及用于回滚的备份。
